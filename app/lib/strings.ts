// NOTE: HAVE IT WHERE AFTER INPUT THE PERSON CAN CONFIRM THE FORMATTED STRING IS WHAT THEY WANT, AND TELL THEM TO ADD PARENTHESES IF NOT

export function formatExpressionString(expressionString: string): string {
  /*
    NOTE:
        C<Name> = Trait (Name)
        T<Name> = Tree (Name)
        P<Name> = Template (Name)
        M<Name> = Move (Name)
  */
  return "hi";
}
